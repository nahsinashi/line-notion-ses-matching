# LINE x Notion SES マッチングシステム

## コミット・プッシュルール（必須）

コード修正が完了したら、**必ずGitコミット＆プッシュまで実行すること**。
- 修正対象のファイルのみをステージングする（関係ないファイルは含めない）
- コミットメッセージは日本語で、変更内容と理由がわかるようにする
- mainブランチに直接プッシュしてよい

## GAS デプロイ手順ガイド

このプロジェクトはGAS（Google Apps Script）で動作しており、GitHubとGASエディタは連携していない。
コード修正後は手動でGASエディタにコピー＆デプロイが必要。

### デプロイ時のバージョン選択

「デプロイを管理」から既存のデプロイを編集する限り、**URLは変わらない**。
バージョンを新しくしてもURLは同じなので、LINE Developers等の再設定は不要。

| 操作 | URLへの影響 | 用途 |
|------|-----------|------|
| 既存デプロイを編集 → **新しいバージョン** | URLは変わらない | 通常のバグ修正・機能追加（推奨） |
| 既存デプロイを編集 → バージョンそのまま | URLは変わらない | コード変更を反映しない（デプロイ設定のみ変更時） |
| **新しいデプロイを作成** | **URLが変わる** | 別環境を作りたい場合のみ（通常使わない） |

### 具体的な手順（通常のデプロイ）

1. GASエディタでファイルを更新（GitHubのコードをコピペ）
2. 「デプロイ」→「デプロイを管理」
3. 既存のデプロイの鉛筆アイコン（編集）をクリック
4. バージョンを **「新しいバージョン」** に変更
5. 説明欄に変更内容を記載
6. 「デプロイ」

### デプロイ説明文のフォーマット

GASエディタの「説明」欄には以下の形式で記載：

```
[日付] 変更内容の要約
例: [2026-03-02] 承認フローのキャッシュ不整合バグ修正
```

## プロジェクト構成

| ディレクトリ | 役割 | デプロイ先 |
|-------------|------|-----------|
| `gas-main` | マッチングエンジン（案件/要員登録、AIマッチング、LINE通知） | GAS: gas-main プロジェクト |
| `gas-admin` | 管理者向け機能（承認フロー、パートナー返答処理） | GAS: gas-admin プロジェクト |

## Notion DB に関する注意

- Notionのデータベースは他メンバーも使用している
- **プロパティの追加・Selectオプションの新規追加は避ける**
- 既存の選択肢（候補、打診中、提案中、面談、結果待ち、見送り、辞退、決定）のみ使用すること
